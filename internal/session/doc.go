// Package session implements the JSONL append-only session log
// ({timestamp}_{id}.jsonl), kept format-compatible with pi so existing session
// files can be read directly.
//
// Port of pi's core/session-manager. To be implemented in Phase 4.
package session
