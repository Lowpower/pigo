// Package sessionrepo is the v4 session repository contract (SessionRepo /
// SessionStorage / SessionTree). The coding-agent JSONL Manager in
// internal/session is a separate v3 stack and does not implement these types.
package sessionrepo
