// Package version is the pigo CLI version string.
package version

// Version is the current pigo version (--version, changelog, telemetry).
// Release binaries override this with -ldflags -X.
var Version = "0.0.1-dev"
